import { Road_Rage } from "next/font/google";
import { Roboto } from "next/font/google";

export const roboto = Roboto({ weight: ["400", "700"], subsets: ["latin"] });
export const roadRage = Road_Rage({ weight: "400", subsets: ["latin"] });
